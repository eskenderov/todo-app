import React from "react";

const Header = () => {
  return (
    <header
      style={{
        width: "100%",
        height: 60,
        padding: 0,
        marginBottom: 20,
        background: "blue",
        color: "white",
      }}
    ></header>
  );
};

export default Header;
